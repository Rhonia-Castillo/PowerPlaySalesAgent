# Sales-expert Tag Activation

Adding the exact `Sales-expert` tag is an explicit activation request for the current ticket. Processing must be idempotent when ticket creation was already handled.

Implementation status: blocked pending a validated Tokity tag-change event and authorization evidence.
